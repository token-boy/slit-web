export const IS_DEV = process.env.NODE_ENV === "development";

export const CHIPS_RATE = 1000n;

export const SOL_DECIMALS = BigInt(Math.pow(10, 9))

export const CDN_URL = 'https://files.mxsyx.site'
